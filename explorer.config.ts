const explorer = {
    clusters: [
        {
            cluster: 'zumanet',
            name: 'Zumanet',
            uri: 'http://localhost:8899', // change this to point to your own cluster
        },
    ],
    features: {
        pingStats: false,
    },
};

export default explorer;
